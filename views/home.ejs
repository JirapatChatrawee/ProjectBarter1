<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    
    <!-- Responsive -->
    <style>
        .navbar-brand img {
            height: 100px; /* ปรับความสูงของโลโก้ */
            width: 250px; /* ให้ความกว้างปรับตามสัดส่วนของโลโก้ */
        }
        .navbar-nav .nav-link {
            font-size: 1.5rem; /* ปรับขนาดตัวอักษรใหญ่ขึ้น */
            margin-left: 2rem; /* ขยับไปทางขวา */
        }
        .navbar-text {
            font-size: 1.5rem; /* ปรับขนาดตัวอักษรใหญ่ขึ้น */
        }
        .card {
            height: 100%; /* ให้การ์ดมีความสูงเต็มที่เท่ากัน */
            display: flex;
            flex-direction: column;
        }
        .card-body {
            flex: 1;
            display: flex;
            flex-direction: column;
        }
        .card-title, .card-text {
            font-family: 'Arial', sans-serif; /* ปรับฟอนต์ */
        }
        .card-text strong {
            color: orange; /* สีสถานะเป็นสีส้ม */
        }
        .card-img-top {
            object-fit: cover; /* ให้รูปภาพเต็มพื้นที่ */
            height: 200px; /* ความสูงของรูปภาพ */
        }
    </style>
</head>
<body>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/"><img src="/images/Logo.png" alt="Logo"></a>
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link" href="upload">ลงสิ่งของ</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="save-offer">บันทึกข้อเสนอ</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="notifications">การแจ้งเตือน</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="settings">การตั้งค่า</a>
                    </li>
                </ul>
                <span class="navbar-text me-3">
                    <%= name %>
                </span>
                <a href="/logout" class="btn btn-danger">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container py-5">
        <h2 class="text-center mb-4">สินค้า</h2>
        <div class="row">
            <% products.forEach(product => { %>
                <div class="col-md-4 mb-4">
                    <div class="card">
                        <img src="<%= product.image %>" class="card-img-top" alt="<%= product.name %>">
                        <div class="card-body">
                            <h5 class="card-title"><%= product.name %></h5>
                            <p class="card-text"><%= product.description %></p>
                           <!-- <p class="card-text"><strong>ที่อยู่:</strong> <%= product.location %> </p> -->
                            <p class="card-text"><strong></strong> <span style="color: orange;"><%= product.status === 'free' ? 'Free' : 'Exchange' %></span></p>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>
